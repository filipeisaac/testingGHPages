/** @type {import('next').NextConfig} */
const nextConfig = {
    //basePath: "/testingGHPages",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };

export default nextConfig;
